@using MS.Business;
@using MS.Web.Models;
@using MS.Web.Code.LIBS;
@using MS.Core;
@using System.Web.Mvc;
@using System.Web.Optimization;
@using MS.Core.Language;

<!DOCTYPE html>
<html>
<head>
    <title>@ViewBag.Title</title>
    <meta content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' name='viewport'>

    <script type="text/javascript">
        var siteAdminDomain = '@SiteKeys.Domain';
    </script>
   
    @Styles.Render("~/Contents/css")

    @Scripts.Render("~/bundles/modernizr")
    @RenderSection("HeadContent", false)
</head>
<body class="skin-black">
    <header class="header">
        <a href="@Url.Action("","")" class="logo">
            @Resource_tr_TR.Migros
        </a>
        <nav class="navbar navbar-static-top" role="navigation">
            <a role="button" data-toggle="offcanvas" class="navbar-btn sidebar-toggle" href="#">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
            <div class="navbar-right">
                <ul class="nav navbar-nav">
                    <li class="dropdown messages-menu">
                        @*<a href="@Url.Content("~/admin/request")">
                            <i class="fa fa-envelope"></i>
                            @if (MS.Business.Request.GetNewRequest().Count > 0)
                            {
                                <span class="label bg-yellow" title="New Request">@MS.Business.Request.GetNewRequest().Count</span>
                            }
                        </a>*@
                    </li>
                    <li class="dropdown user user-menu">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <i class="glyphicon glyphicon-user"></i>
                         @if (SiteSession.TblKullanicilar != null)
                         { 
                            <span>@SiteSession.TblKullanicilar.Isim<i class="caret"></i></span>
                         }
                        </a>
                        <ul class="dropdown-menu">
                            <li class="user-header bg-black">
                                <img src="@Url.Content("~/areas/admin/content/images/usr.png")" class="img-circle" alt="User Image" />
                                <p>
                               @if(SiteSession.TblKullanicilar != null)
                               {
                                    <a href="" class="white">@SiteSession.TblKullanicilar.Isim</a>
                               }
                                </p>
                            </li>
                            <li class="user-footer">
                                <div class="pull-right">
                                    <a href="@Url.Action("Logout", "Home")" class="btn btn-default btn-flat">@Resource_tr_TR.Signout</a>
                                </div>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </nav>
    </header>
    <div class="wrapper row-offcanvas row-offcanvas-left">
         @Html.Partial("_AdminLeft")        
            <aside class="right-side">

                @RenderBody()

            </aside>
        </div>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueri")
    @Scripts.Render("~/bundles/modernizr")
      
  
        <script type="text/javascript">
            function SetSubMenu(menuname) {
                $("#" + menuname).addClass("active");
                $("#" + menuname).parents(".treeview").addClass("active");
            }
        </script>

      
    @RenderSection("ScriptContent", false)
</body>
</html>
